name: Spark Consumer & Processing

on:
  pull_request:
    types:
      - closed
  push:
    branches:
      - main
    #   - ci-cd
    
jobs:
  deployment:
    name: Service Deployment
    runs-on: ubuntu-latest
    steps:
      - name: Template
        run: echo "Template"
    
