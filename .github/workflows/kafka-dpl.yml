name: Kafka Producer

on:
  pull_request:
    types:
      - closed
  push:
    branches:
      - main
    #   - ci-cd

jobs:
  deployment: Service Deployment
    name: Kafka
    runs-on: ubuntu-latest
    steps:
      - name: Template
        run: echo "Template"
    